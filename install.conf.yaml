- defaults:
    link:
      create: true
      relink: true
      force: true

- clean: ["~"]

- create:
    - ~/projects
    - ~/scripts

- shell:
    -
      command: chmod +x ./scripts/install_dependencies.sh
      description: Give install script executable permissions
    -
      command: ./scripts/install_dependencies.sh
      description: Run install script for install_dependencies
      stdout: true

- link:
    ~/scripts:
    ~/.config:
      glob: true
      path: config/**
    ~/:
      glob: true
      path: git/*
      prefix: '.'
